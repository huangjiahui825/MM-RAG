# 4.21 注销：
# MULTIMODAL_COMBINATION_PROMPT_TEMPLATE = """你是一个资深的全屋定制与室内软装设计师。用户希望搭配一套家具组合。以下是用户针对不同家具选品的查询需求，以及系统为每个需求检索到的候选素材池。

# ### 任务要求：
# 1. **精准匹配**：请你综合考虑用户的描述意图、尺寸要求、材质偏好、风格颜色，从每个查询的候选池中挑选出最合适的一项。
# 2. **组合协调**：确保你挑选出的所有家具组合在一起时，在视觉风格（如现代简约、北欧、奶油风等）、色彩搭配和空间功能上是高度协调互补的。
# 3. **输出格式**：
#    请严格按以下 Markdown 格式输出你的推荐方案：
   
#    #### [整体搭配理念]
#    （详细阐述这套家具组合的整体风格、色彩基调，以及为什么这样搭配能满足用户的需求）

#    #### [各组件推荐]
#    - **组件1 (对应查询1)**: 
#      - **推荐素材ID**: [填入 ID]
#      - **素材名称**: [填入 名称]
#      - **素材URL**: [填入 URL]
#      - **推荐理由**: [详细说明：1. 为什么它符合该查询的具体要求；2. 它如何与组合中的其他家具相呼应]
#    - **组件2 (对应查询2)**:
#      ... (以此类推)

# ### 用户的查询与候选素材池：
# {all_queries_context}
# """

VLM_SELECT_ASSETIDS_PROMPT = """
你是一个资深的全屋定制与室内软装设计师。用户希望搭配一套家具组合。
以下是用户针对不同家具选品的查询需求，以及系统为每个需求检索到的候选素材池。

### 任务要求：
1. **精准匹配**：请你综合考虑用户的描述意图、尺寸要求，从每个查询的候选池中挑选出最合适的一项。
2. **组合协调**：确保你挑选出的所有家具组合在一起时，在视觉风格、色彩搭配和空间功能上是高度协调互补的。
3. **输出格式**：
   - 仅输出一个 JSON 数组，数组中的每个元素必须是一个包含 "query_index" 和 "assetid" 字段的对象。
   - 绝对不要输出任何解释性文字、Markdown 标记（如 ```json）或其他多余内容。
   - 数组中的元素数量应与查询的数量一致（每个查询挑选一个）。

### 用户的查询与候选素材池：
{all_queries_context}

示例输出格式:
[
  {{"query_index": 1, "assetid": "123456"}},
  {{"query_index": 2, "assetid": "789012"}}
]
"""

